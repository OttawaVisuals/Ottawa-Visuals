# Canada Geothermal Feasibility (Energy repo companion)

**Status:** 🧪 exploration (~15%), no visual yet.

Ground-source heat pump / geothermal feasibility mapping using GSC bedrock
geology and NRCan open-file well data. This is the Ottawa Visuals side of a
project whose main build lives in the separate `Energy` repo/site
(`C:\Energy` — see [OttawaVisuals/Energy](https://github.com/OttawaVisuals/Energy)).

The `heatpump/` folder here is currently an empty placeholder pending that
work being scoped into this repo.
